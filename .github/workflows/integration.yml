name: integration-tests

on:
  push:
  pull_request:

jobs:
  integration:
    runs-on: ubuntu-latest

    env:
      GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Clean install
        run: |
          pip uninstall -y cmc-csci40-MeganTu || true
          pip install .

      - name: Run integration test
        run: |
          chat <<'EOF'
          I am bob.
          What is my name?
          EOF